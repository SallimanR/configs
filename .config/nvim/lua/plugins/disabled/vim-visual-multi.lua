return {
  'mg979/vim-visual-multi',
  lazy = false, -- Load immediately (optional)
  init = function()
    -- Optional: Add keymaps or plugin-specific configurations here
    -- Example: Change default multi-cursor keybind (see plugin docs for options)
  end,
}
